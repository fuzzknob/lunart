import 'package:lunart/lunart.dart';
import 'package:basic_example/controllers/app_controller.dart';

Router bootstrapRoutes() {
  final routes = Router();

  // routes.get('', (AppController.new, 'index'));

  return routes;
}
